export default function SettingsPage() {
  return (
    <div className="settings-page">
      <h1>Settings</h1>
      <p className="page-message">Default model and permissions — to be wired when backend supports.</p>
    </div>
  );
}
